name: release

on:
  release:
    types: [edited, published]

env:
  NODE: '12.20.1'
  METEOR: '2.0'

jobs:
  # lint:
  #   runs-on: ubuntu-20.04
  #   steps:
  #     - name: Checking repo out
  #       uses: actions/checkout@v1
  #     - name: Setup Node
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: ${{ env.NODE }}
  #     - name: Install dependencies
  #       run: npm ci
  #     - name: Linting
  #       run: npm run lint

  # test:
  #   runs-on: ubuntu-20.04
  #   steps:
  #     - name: Checking repo out
  #       uses: actions/checkout@v1
  #     - name: Setup Meteor
  #       uses: meteorengineer/setup-meteor@v1
  #       with:
  #         meteor-release: ${{ env.METEOR }}
  #     - name: Install dependencies
  #       run: meteor npm install
  #     - name: Run tests
  #       run: npm run test

  deploy:
    runs-on: ubuntu-20.04
    # needs: [lint, test]
    steps:
      - name: Checking repo out
        uses: actions/checkout@v1

      - name: Setup Meteor
        uses: meteorengineer/setup-meteor@v1
        with:
          meteor-release: ${{ env.METEOR }}

      - name: Install dependencies
        run: meteor npm install

      - name: Build application
        run: npm run build

      - name: Get version number
        id: get_version
        run: |
          echo ::debug::${{ github.repository }}
          echo ::debug::${{ github.repository_owner }}
          echo ::debug::${{ github.event.repository.name }}
          echo ::debug::SRC = ${{ steps.get_version.outputs.name }}.tar.gz
          echo ::debug::DST = recorder-${{ steps.get_version.outputs.tag }}.tar.gz

          TAG=$(echo ${{ github.ref }} | cut -d'/' -f 3)
          echo ::set-output name=TAG::${TAG}
          echo ::set-output name=REPO::${{ github.repository }}
          echo ::set-output name=NAME::${{ github.event.repository.name }}
          echo ::set-output name=SRC::${{ steps.get_version.outputs.name }}.tar.gz
          echo ::set-output name=DST::recorder-${{ steps.get_version.outputs.tag }}.tar.gz
        shell: bash

      - name: Upload application bundle to the artifacts
        uses: actions/upload-artifact@v2
        with:
          name: ${{ steps.get_version.outputs.dst }}
          path: ${{ steps.get_version.outputs.src }}

      - name: Retrieve UPLOAD_URL
        id: get_release
        run: |
          upload_url=$(curl -sL https://api.github.com/repos/${{ steps.get_version.outputs.repo }}/releases/tags/${{ steps.get_version.outputs.tag }} | jq -r '.upload_url')
          echo ::set-output name=UPLOAD_URL::$upload_url
        shell: bash

      - name: Upload Release Assets
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.get_release.outputs.upload_url }}
          asset_path: ${{ steps.get_version.outputs.src }}
          asset_name: ${{ steps.get_version.outputs.dst }}
          asset_content_type: application/gzip
